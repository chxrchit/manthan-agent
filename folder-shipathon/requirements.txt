# MANTHA Pipeline — Python dependencies
pandas>=2.0
openpyxl>=3.1           # Excel read/write
xlrd>=2.0               # Legacy .xls support
requests>=2.31          # OpenRouter API calls
matplotlib>=3.8
seaborn>=0.13
reportlab>=4.1          # PDF generation

# Gmail API (optional — falls back to SMTP if not installed)
google-auth>=2.28
google-auth-oauthlib>=1.2
google-api-python-client>=2.120